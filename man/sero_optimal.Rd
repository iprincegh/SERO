% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sero_optimal.R
\name{sero_optimal}
\alias{sero_optimal}
\title{Calculate optimal emergency response locations using multi-criteria analysis}
\usage{
sero_optimal(
  data,
  grid_size = 100,
  risk_categories = c(1, 2),
  min_road_distance = 500,
  max_road_distance = 1000,
  max_locations = 10,
  target_crs = 25832,
  avoid_landuse = TRUE,
  weights = list(accident_weight = 0.4, population_weight = 0.3, road_weight = 0.3),
  min_accidents = 1,
  custom_scoring_function = NULL,
  landuse_types = NULL,
  population_threshold = 0,
  verbose = TRUE
)
}
\arguments{
\item{data}{List containing spatial data layers}

\item{grid_size}{Grid cell size in meters (default=100)}

\item{risk_categories}{High-risk accident categories (default=c(1,2))}

\item{min_road_distance}{Minimum distance from roads in meters (default=500)}

\item{max_road_distance}{Maximum distance from roads in meters (default=1000)}

\item{max_locations}{Maximum number of locations to return (default=10)}

\item{target_crs}{Target CRS for calculations (default=25832 for UTM Zone 32N)}

\item{avoid_landuse}{Logical, whether to avoid existing land use areas (default=TRUE)}

\item{weights}{Named list of scoring weights: accident_weight, population_weight, road_weight (default: c(0.4, 0.3, 0.3))}

\item{min_accidents}{Minimum number of accidents per grid cell to consider (default=1)}

\item{custom_scoring_function}{Optional custom function for scoring (default=NULL)}

\item{landuse_types}{Character vector of land use types to avoid (default=NULL for all types)}

\item{population_threshold}{Minimum population density threshold (default=0)}

\item{verbose}{Logical, whether to show progress messages (default=TRUE)}
}
\value{
sero_optimal_locations S3 object
}
\description{
Implements complete multi-criteria analysis for finding optimal emergency service locations:
\enumerate{
\item High-risk accidents (UKATEGORIE = 1 or 2)
\item Areas NOT within existing land use polygons (avoiding developments)
\item Road proximity (customizable distance range)
\item Accident density analysis using grid cells
\item Population density (higher density prioritized)
\item Optimal positioning within suitable areas
}
}
\examples{
\dontrun{
# Basic usage
result <- sero_optimal(data)

# Custom weights (prioritize accidents)
result <- sero_optimal(data, 
  weights = list(accident_weight = 0.6, population_weight = 0.2, road_weight = 0.2))

# Urban analysis (closer to roads)
result <- sero_optimal(data, 
  min_road_distance = 200, max_road_distance = 600)

# Custom scoring function
my_scoring <- function(data) {
  data$weights$accident_weight * data$accident_density_norm^2 +
  data$weights$population_weight * data$population_density_norm +
  data$weights$road_weight * data$road_accessibility_norm
}
result <- sero_optimal(data, custom_scoring_function = my_scoring)
}
}
