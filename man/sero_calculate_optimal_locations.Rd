% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sero_optimal.R
\name{sero_calculate_optimal_locations}
\alias{sero_calculate_optimal_locations}
\title{Calculate optimal emergency response locations using enhanced multi-criteria analysis}
\usage{
sero_calculate_optimal_locations(
  data,
  grid_size = 100,
  risk_categories = c(1, 2),
  suitable_landuse = c("residential", "commercial", "industrial"),
  min_road_distance = 500,
  max_road_distance = 1000,
  max_locations = 10,
  target_crs = 25832
)
}
\arguments{
\item{data}{List containing spatial data layers}

\item{grid_size}{Grid cell size in meters (default=100)}

\item{risk_categories}{High-risk accident categories (default=c(1,2))}

\item{suitable_landuse}{Suitable land use classes (default=c("residential", "commercial", "industrial"))}

\item{min_road_distance}{Minimum distance from roads in meters (default=500)}

\item{max_road_distance}{Maximum distance from roads in meters (default=1000)}

\item{max_locations}{Maximum number of locations to return (default=10)}

\item{target_crs}{Target CRS for calculations (default=25832 for UTM Zone 32N)}
}
\value{
sero_optimal_locations S3 object
}
\description{
This function implements the complete multi-criteria analysis for finding optimal
emergency service locations based on the specified criteria:
\enumerate{
\item High-risk accidents (UKATEGORIE = 1 or 2)
\item Suitable land use (residential, commercial, industrial)
\item Road proximity (500m-1000m from roads)
\item Accident density (100m grid cells)
\item Population density (higher density prioritized)
\item Centroid calculation (optimal points within grid cells)
}
}
