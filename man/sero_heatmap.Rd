% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sero_heatmap.R
\name{sero_heatmap}
\alias{sero_heatmap}
\title{Create enhanced accident heatmap with customizable features}
\usage{
sero_heatmap(
  accidents,
  risk_categories = c("fatal", "severe"),
  risk_category_names = NULL,
  bandwidth = 1000,
  grid_size = 100,
  data = NULL,
  clip_to_munster = TRUE,
  basemap = "districts",
  show_landuse = FALSE,
  show_accidents = TRUE,
  color_scheme = "viridis",
  custom_colors = NULL,
  alpha_heatmap = 0.8,
  alpha_basemap = 0.3,
  kernel = "gaussian",
  normalize_density = FALSE,
  districts = NULL,
  bins = NULL
)
}
\arguments{
\item{accidents}{sf object containing accident data with UKATEGORIE column}

\item{risk_categories}{numeric vector, categories to include (1=fatal, 2=severe, 3=slight)}

\item{risk_category_names}{character vector, alternative to risk_categories ("fatal", "severe", "slight")}

\item{bandwidth}{numeric, bandwidth for kernel density estimation in meters (default: 1000)}

\item{grid_size}{numeric, grid cell size in meters (default: 100)}

\item{data}{list, SERO data object containing districts and other spatial layers}

\item{clip_to_munster}{logical, whether to clip heatmap to Munster boundaries (default: TRUE)}

\item{basemap}{character, basemap type: "districts", "osm", or "none" (default: "districts")}

\item{show_landuse}{logical, whether to show land use overlay (default: FALSE)}

\item{show_accidents}{logical, whether to show individual accident points (default: TRUE)}

\item{color_scheme}{character, color scheme: "viridis", "plasma", "inferno", "magma", "custom" (default: "viridis")}

\item{custom_colors}{character vector, custom color palette when color_scheme="custom"}

\item{alpha_heatmap}{numeric, transparency of heatmap layer (0-1, default: 0.8)}

\item{alpha_basemap}{numeric, transparency of basemap layer (0-1, default: 0.3)}

\item{kernel}{character, kernel type: "gaussian", "epanechnikov", "quartic" (default: "gaussian")}

\item{normalize_density}{logical, whether to normalize density values (default: FALSE)}

\item{districts}{DEPRECATED - use data parameter instead}

\item{bins}{DEPRECATED - use grid_size parameter instead}
}
\value{
ggplot2 object (enhanced heatmap)
}
\description{
This function creates sophisticated heatmaps of accident data with support for:
\itemize{
\item Multiple basemap options (districts, OSM, none)
\item Clipping to Munster city boundaries
\item Custom color schemes and transparency controls
\item Kernel density estimation with various kernels
}
}
\examples{
\dontrun{
data <- sero_load_data()

# Basic heatmap with districts basemap (fatal and severe accidents)
heatmap_plot <- sero_heatmap(data$accident)

# Only fatal accidents with OSM basemap and custom colors
heatmap_plot <- sero_heatmap(data$accident, 
                            risk_categories = "fatal",
                            basemap = "osm",
                            color_scheme = "custom",
                            custom_colors = c("white", "yellow", "orange", "red", "darkred"),
                            bandwidth = 800, 
                            grid_size = 50)

# All accident types with plasma color scheme and clipping
heatmap_plot <- sero_heatmap(data$accident, 
                            risk_categories = c("fatal", "severe", "slight"),
                            color_scheme = "plasma",
                            clip_to_munster = TRUE,
                            alpha_heatmap = 0.7)
}
}
